# Настройка Telegram бота для DarkCase

## Шаг 1: Создание бота в BotFather

1. Откройте [@BotFather](https://t.me/BotFather)
2. Отправьте `/newbot`
3. Следуйте инструкциям и создайте бота
4. **Скопируйте токен бота** (выглядит как `123456789:ABCdefGHIjklMNOpqrsTUVwxyz`)

## Шаг 2: Настройка WebApp в BotFather

1. Отправьте `/newapp` в BotFather
2. Выберите вашего бота
3. Заполните:
   - **Title**: `DarkCase`
   - **Description**: `Streaming platform`
   - **Photo**: загрузите иконку 512x512px
   - **Web App URL**: `https://darkcase.vercel.app`
   - **Short name**: `darkcase`

4. **КРИТИЧЕСКИ ВАЖНО**: В настройках WebApp выберите **Fullscreen mode** (не Compact или Medium)

## Шаг 3: Установка зависимостей

```bash
npm install
```

## Шаг 4: Создание .env файла

Создайте файл `.env` в корне проекта:

```env
BOT_TOKEN=ваш_токен_от_BotFather
```

## Шаг 5: Запуск бота

```bash
npm run bot
```

Вы должны увидеть:
```
🤖 DarkCase Bot is running...
📱 Use /start or /app to open the WebApp
```

## Шаг 6: Тестирование

1. Найдите вашего бота в Telegram
2. Отправьте `/start` или `/app`
3. Нажмите на кнопку **"Open DarkCase"**
4. Приложение должно открыться **полноэкранно** без серой шапки Telegram

## ✅ Проверка полноэкранного режима

Приложение открывается правильно, если:
- ✅ Нет серой шапки Telegram сверху
- ✅ Приложение занимает весь экран
- ✅ Нельзя свернуть свайпом вниз (только через кнопку Close)
- ✅ Нет белой вспышки при загрузке

## 🔧 Изменение URL

Если ваш WebApp размещён на другом домене, измените URL в `bot.js`:

```javascript
web_app: { url: 'https://your-domain.com' }
```

## 🐛 Решение проблем

### Проблема: WebApp открывается как панель, а не полноэкранно

**Решение:**
1. Проверьте в BotFather, что для WebApp установлен режим **Fullscreen**
2. Убедитесь, что используется именно `web_app: { url: ... }`, а не обычная URL кнопка
3. Перезапустите бота после изменений

### Проблема: Бот не отвечает

**Решение:**
1. Проверьте, что токен в `.env` правильный
2. Убедитесь, что бот запущен (`npm run bot`)
3. Проверьте логи на ошибки

### Проблема: Кнопка не работает

**Решение:**
1. Убедитесь, что URL начинается с `https://`
2. Проверьте, что WebApp доступен по указанному URL
3. Убедитесь, что в BotFather WebApp настроен и активирован

