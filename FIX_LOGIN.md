# 🔧 Исправление ошибки авторизации (404)

## Проблема

Ошибка 404 при попытке входа означает, что Backend не находит роут `/api/admin/auth/login`.

## ✅ Решение

### Шаг 1: Убедитесь, что Backend запущен

Проверьте, что Backend запущен на порту 3000:

```bash
cd backend
npm run dev
```

Должно появиться:
```
🚀 DarkCase API server running on port 3000
📋 Registered API routes:
   POST /api/admin/auth/login
```

### Шаг 2: Проверьте, что Backend доступен

Откройте в браузере:
```
http://localhost:3000/api/admin/auth/login
```

Должна быть ошибка метода (не 404), что означает, что роут существует.

Или проверьте через curl (если установлен):
```bash
curl http://localhost:3000/health
```

Должен вернуться: `{"status":"ok",...}`

### Шаг 3: Проверьте логи Backend

При попытке входа в админ-панели, в терминале Backend должны появиться логи:
```
🔐 Admin login attempt: { username: 'admin' }
✅ Login data validated: { username: 'admin' }
```

Если этих логов нет - запрос не доходит до Backend.

### Шаг 4: Проверьте прокси Vite

Убедитесь, что в `admin-panel/vite.config.ts` настроен прокси:

```typescript
proxy: {
  '/api': {
    target: 'http://localhost:3000',
    changeOrigin: true,
  },
}
```

### Шаг 5: Перезапустите оба сервиса

**Терминал 1 - Backend:**
```bash
cd backend
npm run dev
```

**Терминал 2 - Админ-панель:**
```bash
cd admin-panel
npm run dev
```

### Шаг 6: Откройте консоль браузера

Нажмите F12 → вкладка Console. При попытке входа должны появиться логи:
```
🔧 API URL: (using Vite proxy)
🔐 Attempting login for: admin
📡 API URL: (relative)
📡 Full endpoint: /api/admin/auth/login
```

Если видите ошибку 404, проверьте:
- Запущен ли Backend
- Правильно ли работает прокси

## 🔍 Диагностика

### Проверка 1: Backend запущен?

```bash
# Windows
netstat -ano | findstr :3000

# Должен показать процесс на порту 3000
```

### Проверка 2: Роут существует?

Откройте в браузере:
```
http://localhost:3000/api/admin/auth/login
```

Если видите `{"error":"Route not found"}` - роут не зарегистрирован.
Если видите другую ошибку - роут существует, но есть другая проблема.

### Проверка 3: Прокси работает?

В консоли браузера (F12 → Network) при попытке входа:
- Запрос должен идти на `/api/admin/auth/login` (относительный путь)
- Статус должен быть 200 (не 404)
- Если 404 - прокси не работает или Backend не запущен

## ✅ После исправления

После перезапуска обоих сервисов:
1. Откройте админ-панель: `http://172.20.10.2:3001`
2. Войдите с `admin` / `admin123`
3. Должно работать!

