# 🔧 Исправление ошибки "Port 3000 already in use"

## Проблема

Ошибка `EADDRINUSE: address already in use :::3000` означает, что порт 3000 уже занят другим процессом.

## ✅ Решение

### Вариант 1: Найти и остановить процесс (рекомендуется)

#### Windows:

1. **Найдите процесс:**
   ```bash
   netstat -ano | findstr :3000
   ```
   
   Вы увидите что-то вроде:
   ```
   TCP    0.0.0.0:3000           0.0.0.0:0              LISTENING       12345
   ```
   
   Где `12345` - это PID процесса.

2. **Остановите процесс:**
   ```bash
   taskkill /PID 12345 /F
   ```
   
   Замените `12345` на реальный PID из предыдущей команды.

3. **Запустите Backend снова:**
   ```bash
   npm run dev
   ```

### Вариант 2: Изменить порт Backend

Если не хотите останавливать процесс на порту 3000:

1. **Создайте или отредактируйте `backend/.env`:**
   ```env
   PORT=3002
   ```

2. **Обновите `admin-panel/vite.config.ts`:**
   ```typescript
   proxy: {
     '/api': {
       target: 'http://localhost:3002', // Измените на новый порт
       changeOrigin: true,
     },
   }
   ```

3. **Перезапустите оба сервиса**

### Вариант 3: Использовать другой порт временно

В `backend/src/index.ts` можно временно изменить:
```typescript
const PORT = process.env.PORT || 3002 // Измените на 3002
```

Но лучше использовать `.env` файл.

## 🔍 Быстрая команда для Windows

```powershell
# Найти и убить процесс на порту 3000
$port = 3000
$process = Get-NetTCPConnection -LocalPort $port -ErrorAction SilentlyContinue
if ($process) {
    $pid = $process.OwningProcess
    Stop-Process -Id $pid -Force
    Write-Host "Process $pid stopped"
} else {
    Write-Host "No process found on port $port"
}
```

## ✅ После исправления

1. Убедитесь, что порт свободен:
   ```bash
   netstat -ano | findstr :3000
   ```
   
   Не должно быть вывода (порт свободен).

2. Запустите Backend:
   ```bash
   cd backend
   npm run dev
   ```

3. Должно появиться:
   ```
   🚀 DarkCase API server running on port 3000
   ```

