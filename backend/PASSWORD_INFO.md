# 🔐 Информация о паролях

## ✅ Пароли обновлены!

В файле `backend/.env` были сгенерированы безопасные пароли:

### 🔑 Админ-пароль

**Важно:** Пароль хранится в файле `backend/.env` в переменной `ADMIN_PASSWORD`.

Чтобы увидеть пароль:
```bash
cd backend
cat .env | grep ADMIN_PASSWORD
```

Или откройте файл `backend/.env` в редакторе и найдите строку:
```
ADMIN_PASSWORD=ваш_пароль_здесь
```

### 🔒 JWT Secret

Также был сгенерирован безопасный `JWT_SECRET` для подписи токенов.

## 📝 Как использовать

1. **Откройте** `backend/.env`
2. **Найдите** строку `ADMIN_PASSWORD=...`
3. **Скопируйте** пароль
4. **Используйте** его для входа в админ-панель:
   - Username: `admin` (или значение из `ADMIN_USERNAME`)
   - Password: значение из `ADMIN_PASSWORD`

## ⚠️ Безопасность

- **НЕ коммитьте** файл `.env` в Git (он уже в `.gitignore`)
- **НЕ делитесь** паролем публично
- **Измените** пароль на production сервере
- **Используйте** менеджер паролей для хранения

## 🔄 Изменить пароль вручную

Откройте `backend/.env` и измените:
```env
ADMIN_PASSWORD=ваш_новый_безопасный_пароль
```

Затем перезапустите Backend:
```bash
npm run dev
```

