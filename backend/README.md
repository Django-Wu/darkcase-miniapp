# DarkCase Backend

Backend API for DarkCase Telegram Mini App

## 🚀 Быстрый старт

### Требования
- Node.js 18+
- MySQL 5.7+ или MariaDB 10.3+ (или доступ к MySQL на хостинге)
- npm или yarn

### Установка

1. Установите зависимости:
```bash
npm install
```

2. Создайте файл `.env` на основе `env.example`:
```bash
cp env.example .env
```

3. Настройте переменные окружения в `.env`:
```env
PORT=3000
# MySQL Database
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=your_password
DB_NAME=darkcase
TELEGRAM_BOT_TOKEN=your-telegram-bot-token
FILE_STORAGE_TYPE=local
CORS_ORIGIN=http://localhost:5173
```

4. Создайте базу данных MySQL:
```sql
CREATE DATABASE darkcase CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
```

5. Запустите миграции:
```bash
npm run migrate
```

Или миграции запустятся автоматически при старте сервера в development режиме.

6. Запустите сервер:
```bash
# Development режим с hot reload
npm run dev

# Production режим
npm run build
npm start
```

## 📁 Структура проекта

```
backend/
├── src/
│   ├── db/              # База данных и миграции
│   ├── middleware/      # Express middleware
│   ├── routes/          # API routes
│   ├── utils/           # Утилиты (file storage, migrations)
│   └── index.ts         # Точка входа
├── uploads/             # Загруженные файлы (создается автоматически)
└── dist/                # Скомпилированный код
```

## 🔌 API Endpoints

### Публичные (без авторизации)

- `GET /api/cases` - Получить список кейсов
- `GET /api/cases/featured` - Получить главный кейс
- `GET /api/cases/:id` - Получить кейс по ID
- `GET /api/cases/:id/similar` - Получить похожие кейсы
- `GET /api/categories` - Получить все категории
- `GET /api/search?q=query` - Поиск кейсов

### Защищенные (требуют Telegram auth)

- `GET /api/users/me` - Получить текущего пользователя
- `GET /api/users/me/favorites` - Получить избранное
- `POST /api/users/me/favorites` - Добавить в избранное
- `DELETE /api/users/me/favorites/:id` - Удалить из избранного
- `GET /api/users/me/history` - Получить историю просмотра
- `POST /api/users/me/history` - Сохранить прогресс просмотра
- `POST /api/upload/poster` - Загрузить постер
- `POST /api/upload/backdrop` - Загрузить backdrop
- `POST /api/upload/video` - Загрузить видео

### Админ (требуют JWT)

- `POST /api/cases` - Создать кейс
- `PUT /api/cases/:id` - Обновить кейс
- `DELETE /api/cases/:id` - Удалить кейс
- `POST /api/categories` - Создать категорию
- `PUT /api/categories/:id` - Обновить категорию
- `DELETE /api/categories/:id` - Удалить категорию

## 🔐 Авторизация

### Telegram Mini App
Использует `X-Telegram-Init-Data` header:
```
X-Telegram-Init-Data: user=%7B%22id%22%3A123%7D&auth_date=1234567890&hash=...
```

### Admin Panel
Использует JWT токен:
```
Authorization: Bearer {jwt_token}
```

## 💾 File Storage

Поддерживаются три типа хранилища:

1. **Local** (по умолчанию) - файлы сохраняются в `uploads/`
2. **S3** - Amazon S3
3. **R2** - Cloudflare R2

Настройка в `.env`:
```env
FILE_STORAGE_TYPE=local  # или s3, r2
UPLOAD_PATH=./uploads
CDN_URL=http://localhost:3000
```

## 🗄️ База данных

### Миграции

Миграции находятся в `src/db/migrations/` и запускаются автоматически при старте сервера.

Для ручного запуска:
```bash
npm run migrate
```

### Схема базы данных

- `users` - Пользователи Telegram
- `cases` - Криминальные кейсы
- `categories` - Категории кейсов
- `category_cases` - Связь категорий и кейсов
- `user_favorites` - Избранное пользователей
- `user_history` - История просмотра

## 🧪 Тестирование

```bash
# Запуск тестов (когда будут добавлены)
npm test
```

## 📝 Логирование

Логи выводятся в консоль. В production рекомендуется использовать структурированное логирование.

## 🔧 Разработка

### Hot Reload
```bash
npm run dev
```

### Сборка
```bash
npm run build
```

### Линтинг
```bash
npm run lint
```

## 🚀 Деплой

1. Установите переменные окружения на сервере
2. Соберите проект: `npm run build`
3. Запустите: `npm start`

Рекомендуется использовать PM2 или аналогичный процесс-менеджер для production.
