# 🗄️ Настройка MySQL для DarkCase

## 📋 Требования

- MySQL 5.7+ или MariaDB 10.3+
- Доступ к хостингу с MySQL

## 🚀 Быстрая настройка

### 1. Создайте базу данных

На вашем хостинге создайте базу данных:

```sql
CREATE DATABASE darkcase CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
```

### 2. Создайте пользователя (опционально)

```sql
CREATE USER 'darkcase_user'@'%' IDENTIFIED BY 'your_secure_password';
GRANT ALL PRIVILEGES ON darkcase.* TO 'darkcase_user'@'%';
FLUSH PRIVILEGES;
```

### 3. Настройте .env файл

Создайте `backend/.env`:

```env
# Database (MySQL)
DB_HOST=your_host.com
DB_PORT=3306
DB_USER=darkcase_user
DB_PASSWORD=your_secure_password
DB_NAME=darkcase

# Или используйте connection string
# DATABASE_URL=mysql://user:password@host:3306/darkcase
```

### 4. Запустите миграции

```bash
cd backend
npm run migrate
```

Или миграции запустятся автоматически при старте сервера.

## 🔧 Настройка для разных хостингов

### cPanel / phpMyAdmin

1. Войдите в cPanel
2. Откройте "MySQL Databases"
3. Создайте базу данных: `darkcase`
4. Создайте пользователя и назначьте права
5. Используйте данные в `.env`:
   ```env
   DB_HOST=localhost
   DB_USER=your_cpanel_username_darkcase
   DB_PASSWORD=your_password
   DB_NAME=your_cpanel_username_darkcase
   ```

### Cloud Hosting (AWS RDS, DigitalOcean, etc.)

```env
DB_HOST=your-db-host.rds.amazonaws.com
DB_PORT=3306
DB_USER=admin
DB_PASSWORD=your_password
DB_NAME=darkcase
```

### Локальный MySQL

```env
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=your_local_password
DB_NAME=darkcase
```

## 📝 Проверка подключения

После настройки проверьте:

```bash
cd backend
npm run dev
```

Должно появиться:
```
✅ MySQL Database connected
📦 Database is available, running migrations...
✅ All migrations completed
```

## 🔍 Решение проблем

### Ошибка: "Access denied for user"

**Решение:**
- Проверьте правильность логина и пароля
- Убедитесь, что пользователь имеет права на базу данных
- Проверьте, что хост разрешает подключения с вашего IP

### Ошибка: "Can't connect to MySQL server"

**Решение:**
- Проверьте, что MySQL сервер запущен
- Проверьте правильность хоста и порта
- Убедитесь, что firewall разрешает подключения на порт 3306
- Для удаленных хостингов: проверьте, что разрешены внешние подключения

### Ошибка: "Unknown database"

**Решение:**
- Убедитесь, что база данных создана
- Проверьте правильность имени базы в `.env`

### Ошибка при миграциях

**Решение:**
- Убедитесь, что пользователь имеет права CREATE, ALTER, DROP
- Проверьте, что база данных пустая или можно удалить существующие таблицы

## 🔐 Безопасность

1. **Используйте сильные пароли** для пользователя БД
2. **Ограничьте доступ** - разрешайте подключения только с нужных IP
3. **Не храните пароли в коде** - используйте `.env` файл
4. **Используйте SSL** для production подключений

## 📊 Структура базы данных

После миграций будут созданы таблицы:
- `users` - пользователи Telegram
- `cases` - криминальные кейсы
- `categories` - категории
- `category_cases` - связь категорий и кейсов
- `user_favorites` - избранное пользователей
- `user_history` - история просмотров

## ✅ Готово!

После успешной настройки:
1. ✅ База данных создана
2. ✅ Таблицы созданы (миграции выполнены)
3. ✅ Backend подключается к БД
4. ✅ Можно использовать все функции приложения

