# Порты и конфигурация DarkCase

## 📡 Порты приложений

### Основное приложение (Telegram Mini App)
- **Порт**: `5173`
- **URL**: `http://localhost:5173`
- **Команда запуска**: `npm run dev` (в корне проекта)
- **Конфигурация**: `vite.config.ts`

### Админ-панель
- **Порт**: `3001`
- **URL**: `http://localhost:3001`
- **Команда запуска**: `npm run dev` (в папке `admin-panel/`)
- **Конфигурация**: `admin-panel/vite.config.ts`

### Backend API
- **Порт**: `3000`
- **URL**: `http://localhost:3000`
- **Команда запуска**: `npm run dev` (в папке `backend/`)
- **Конфигурация**: `backend/src/index.ts` (переменная `PORT`)

## 🔗 Проксирование

### Основное приложение
Запросы к `/api/*` автоматически проксируются на `http://localhost:3000` через Vite proxy.

### Админ-панель
Запросы к `/api/*` автоматически проксируются на `http://localhost:3000` через Vite proxy.

## 🌐 CORS настройки

Backend разрешает запросы с:
- `http://localhost:5173` (основное приложение)
- `http://localhost:3001` (админ-панель)

Настраивается через переменную окружения `CORS_ORIGIN` в `backend/.env`:
```env
CORS_ORIGIN=http://localhost:5173,http://localhost:3001
```

## 📝 Переменные окружения

### Основное приложение
```env
VITE_API_URL=http://localhost:3000
```

### Админ-панель
```env
VITE_API_URL=http://localhost:3000
```

### Backend
```env
PORT=3000
CORS_ORIGIN=http://localhost:5173,http://localhost:3001
```

## 🚀 Запуск всех сервисов

### Вариант 1: Отдельные терминалы

**Терминал 1 - Backend:**
```bash
cd backend
npm run dev
```

**Терминал 2 - Основное приложение:**
```bash
npm run dev
```

**Терминал 3 - Админ-панель:**
```bash
cd admin-panel
npm run dev
```

### Вариант 2: Concurrent (если установлен)
```bash
npm install -g concurrently
concurrently "cd backend && npm run dev" "npm run dev" "cd admin-panel && npm run dev"
```

## ⚠️ Важные замечания

1. **Backend должен быть запущен первым** - оба фронтенд приложения зависят от него
2. **Порты не должны конфликтовать** - убедитесь, что порты 3000, 3001, 5173 свободны
3. **CORS настройки** - в production добавьте реальные домены в `CORS_ORIGIN`

