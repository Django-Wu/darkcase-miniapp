# 🚀 Быстрый старт DarkCase

## Важно: Какие адреса открывать

### ❌ НЕ открывайте Backend API напрямую
- `http://172.20.10.2:3000` - это **Backend API** (сервер, не интерфейс)
- `http://localhost:3000` - это **Backend API** (сервер, не интерфейс)

### ✅ Откройте админ-панель
- `http://172.20.10.2:3001` - это **Админ-панель** (веб-интерфейс)
- `http://localhost:3001` - это **Админ-панель** (веб-интерфейс)

## 📍 Порты и назначение

| Порт | Сервис | Назначение | URL |
|------|--------|------------|-----|
| 3000 | Backend API | API сервер (не открывать в браузере) | `http://localhost:3000` |
| 3001 | Админ-панель | Веб-интерфейс для управления | `http://localhost:3001` |
| 5173 | Mini App | Telegram Mini App (основное приложение) | `http://localhost:5173` |

## 🎯 Как запустить и использовать

### Шаг 1: Запустите Backend API

```bash
cd backend
npm run dev
```

**Не открывайте этот адрес в браузере!** Это API сервер, не веб-интерфейс.

Вы увидите:
```
🚀 DarkCase API server running on port 3000
   Local:   http://localhost:3000
   Network: http://0.0.0.0:3000
```

### Шаг 2: Запустите админ-панель

В **другом терминале**:

```bash
cd admin-panel
npm run dev
```

Админ-панель автоматически откроется в браузере, или откройте вручную:
- `http://localhost:3001` (если на том же компьютере)
- `http://172.20.10.2:3001` (если с другого устройства в сети)

### Шаг 3: Войдите в админ-панель

**Учетные данные:**
- **Username:** `admin`
- **Password:** `admin123`

## 🔍 Проверка работы

### Проверка Backend API

Откройте в браузере (для проверки):
- `http://localhost:3000` - покажет информацию об API
- `http://localhost:3000/health` - проверит статус

### Проверка админ-панели

Откройте:
- `http://localhost:3001` - должна открыться страница входа

## ⚠️ Частые ошибки

### Ошибка: "Route not found" на порту 3000
- **Причина:** Вы открыли Backend API напрямую
- **Решение:** Откройте админ-панель на порту **3001**

### Ошибка: "Cannot connect to API"
- **Причина:** Backend не запущен
- **Решение:** Запустите Backend на порту 3000

### Ошибка: CORS ошибка
- **Причина:** Backend не разрешает запросы с вашего IP
- **Решение:** Уже исправлено - Backend теперь разрешает локальные IP адреса

### Ошибка: Database connection error
- **Причина:** PostgreSQL не запущен
- **Решение:** Backend может работать без БД для базовых функций (авторизация работает)

## 📝 Порядок запуска

1. ✅ **Сначала** запустите Backend (порт 3000)
2. ✅ **Затем** запустите админ-панель (порт 3001)
3. ✅ **Откройте** админ-панель в браузере (порт 3001, не 3000!)

## 🎉 Готово!

После выполнения всех шагов:
- Backend API работает на порту 3000
- Админ-панель доступна на порту 3001
- Можете войти и управлять контентом

