# DarkCase - Telegram Mini App

Полноценное Telegram Mini App с UI, визуально идентичным мобильному приложению Netflix (учебный проект, без использования логотипов и бренд-графики Netflix).

## 🚀 Технологии

- **React 18** + **TypeScript**
- **Vite** - быстрая сборка и разработка
- **Tailwind CSS** - стилизация
- **Zustand** - управление состоянием
- **React Router** - маршрутизация
- **Telegram WebApp API** - интеграция с Telegram

## 📱 Экраны

Приложение включает следующие экраны:

1. **Onboarding / Login** - экран входа и регистрации
2. **Home** - главный экран с каруселями, подборками и историями просмотра
3. **Search** - поиск с активным инпутом и популярными запросами
4. **Browse** - просмотр по категориям и жанрам
5. **Detail** - детальная информация о фильме/сериале
6. **Video Player** - видеоплеер с поддержкой портретного и ландшафтного режимов
7. **Profile** - профиль пользователя
8. **Settings** - настройки приложения
9. **Error / Loader / Empty State** - состояния ошибок, загрузки и пустых списков
10. **Bottom Navigation** - фиксированная нижняя навигация

## 🎨 Компоненты

UI разбит на атомарные компоненты:

- `Button` - кнопки различных вариантов
- `Input` - поля ввода
- `Modal` - модальные окна
- `CardPoster` - карточки постеров фильмов
- `Carousel` - горизонтальные карусели
- `HorizontalScroller` - горизонтальный скроллер
- `BottomNavigation` - нижняя навигация
- `VideoPlayer` - видеоплеер
- `ProfileSection` - секции профиля
- `SettingRow` - строки настроек
- `Tabs` - вкладки
- `Loader` - индикатор загрузки
- `EmptyState` - пустое состояние
- `Error` - экран ошибки

## 🎨 Цветовая палитра

```css
#000000 - Черный (фон)
#E50914 - Красный (акцент, кнопки)
#222222 - Темно-серый (карточки)
#393939 - Средне-серый (границы)
#6D6D6D - Светло-серый (текст)
#FFFFFF - Белый (основной текст)
```

## 📦 Установка

1. Клонируйте репозиторий:
```bash
git clone <repository-url>
cd DarkCase
```

2. Установите зависимости:
```bash
npm install
```

## 🛠 Разработка

Запустите dev-сервер:

```bash
npm run dev
```

Приложение будет доступно по адресу `http://localhost:3000`

## 🏗 Сборка

Для production сборки:

```bash
npm run build
```

Собранные файлы будут в папке `dist/`

## 📱 Развёртывание в Telegram

### 1. Создание бота

1. Откройте [@BotFather](https://t.me/BotFather) в Telegram
2. Создайте нового бота командой `/newbot`
3. Следуйте инструкциям для создания бота

### 2. Настройка Web App

1. Отправьте команду `/newapp` в BotFather
2. Выберите вашего бота
3. Укажите название и описание приложения
4. Загрузите иконку (512x512px)
5. Укажите URL вашего приложения (после деплоя)

### 3. Деплой

#### Вариант 1: Vercel (рекомендуется)

1. Установите Vercel CLI:
```bash
npm i -g vercel
```

2. Деплой:
```bash
vercel
```

3. Скопируйте полученный URL и укажите его в BotFather

#### Вариант 2: Netlify

1. Установите Netlify CLI:
```bash
npm i -g netlify-cli
```

2. Деплой:
```bash
netlify deploy --prod
```

#### Вариант 3: GitHub Pages

1. Установите `gh-pages`:
```bash
npm install --save-dev gh-pages
```

2. Добавьте в `package.json`:
```json
{
  "scripts": {
    "predeploy": "npm run build",
    "deploy": "gh-pages -d dist"
  }
}
```

3. Деплой:
```bash
npm run deploy
```

### 4. Настройка Telegram WebApp

После деплоя:

1. Откройте BotFather
2. Используйте команду `/myapps` для управления приложениями
3. Выберите ваше приложение
4. Обновите URL на ваш деплой URL

## 🔧 Конфигурация

### Изменение брендинга

Все цвета и размеры вынесены в конфигурацию:

- `src/config/theme.ts` - цвета, отступы, радиусы скругления
- `tailwind.config.js` - конфигурация Tailwind с цветами Netflix

Для изменения брендинга:

1. Откройте `src/config/theme.ts`
2. Измените значения цветов
3. Обновите `tailwind.config.js` соответственно

### Telegram WebApp API

Интеграция с Telegram WebApp API находится в:

- `src/main.tsx` - инициализация при загрузке
- `src/hooks/useTelegramWebApp.ts` - хук для использования API

## 📝 Структура проекта

```
DarkCase/
├── src/
│   ├── components/
│   │   └── ui/          # UI компоненты
│   ├── pages/           # Экраны приложения
│   ├── hooks/           # React хуки
│   ├── store/           # Zustand store
│   ├── config/          # Конфигурация
│   ├── data/            # Mock данные
│   ├── types/           # TypeScript типы
│   ├── App.tsx          # Главный компонент
│   ├── main.tsx         # Точка входа
│   └── index.css        # Глобальные стили
├── public/              # Статические файлы
├── index.html           # HTML шаблон
├── package.json         # Зависимости
├── tsconfig.json        # TypeScript конфигурация
├── vite.config.ts       # Vite конфигурация
├── tailwind.config.js   # Tailwind конфигурация
└── README.md            # Документация
```

## 🎯 Особенности

- ✅ Полная адаптация под Telegram Mini App
- ✅ Поддержка safe area для iOS/Android
- ✅ Отсутствие внешнего скролла страницы
- ✅ Поддержка поворота экрана для видео
- ✅ Корректное отображение в светлой/тёмной теме Telegram
- ✅ Атомарная компонентная структура
- ✅ TypeScript для типобезопасности
- ✅ Responsive дизайн

## 📄 Лицензия

Этот проект создан в учебных целях. Все изображения и контент являются заглушками.

## 🤝 Вклад

Проект создан как базовая структура для дальнейшей разработки. Вы можете:

- Изменить брендинг и цвета
- Добавить реальную функциональность
- Интегрировать с бэкенд API
- Добавить аутентификацию
- Реализовать видеоплеер

## 📞 Поддержка

При возникновении проблем:

1. Проверьте консоль браузера на наличие ошибок
2. Убедитесь, что все зависимости установлены
3. Проверьте конфигурацию Telegram WebApp в BotFather

---

**Примечание**: Это учебный проект. Все изображения и данные являются заглушками. Для production использования необходимо заменить на реальные данные и добавить соответствующую функциональность.

