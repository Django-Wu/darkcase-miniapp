# 🚀 Как запустить админ-панель

## Проблема: ERR_CONNECTION_REFUSED

Если вы видите ошибку "Не удается получить доступ к сайту" на порту 3001, это означает, что админ-панель **не запущена**.

## ✅ Решение: Запустите админ-панель

### Шаг 1: Откройте терминал

Откройте новый терминал (PowerShell, CMD или Git Bash).

### Шаг 2: Перейдите в папку админ-панели

```bash
cd D:\TgPro\DarkCase\admin-panel
```

### Шаг 3: Запустите админ-панель

```bash
npm run dev
```

### Шаг 4: Дождитесь запуска

Вы должны увидеть что-то вроде:

```
  VITE v5.x.x  ready in xxx ms

  ➜  Local:   http://localhost:3001/
  ➜  Network: http://172.20.10.2:3001/
  ➜  press h to show help
```

### Шаг 5: Откройте в браузере

После запуска откройте в браузере:
- **На том же компьютере:** `http://localhost:3001`
- **С другого устройства:** `http://172.20.10.2:3001`

## ⚠️ Важно: Два сервиса должны быть запущены

### 1. Backend API (порт 3000)

В **первом терминале**:
```bash
cd D:\TgPro\DarkCase\backend
npm run dev
```

Должно появиться:
```
🚀 DarkCase API server running on port 3000
```

### 2. Админ-панель (порт 3001)

Во **втором терминале**:
```bash
cd D:\TgPro\DarkCase\admin-panel
npm run dev
```

Должно появиться:
```
➜  Local:   http://localhost:3001/
➜  Network: http://172.20.10.2:3001/
```

## 🔍 Проверка

### Проверка 1: Backend запущен?

Откройте в браузере:
```
http://172.20.10.2:3000
```

Должен показаться JSON с информацией об API (не ошибка).

### Проверка 2: Админ-панель запущена?

Откройте в браузере:
```
http://172.20.10.2:3001
```

Должна открыться страница входа в админ-панель.

## 🐛 Если не работает

### Проблема: "Port 3001 is already in use"

**Решение:** Другой процесс использует порт 3001.

1. Найдите процесс:
   ```bash
   # Windows
   netstat -ano | findstr :3001
   ```

2. Закройте процесс или измените порт в `admin-panel/vite.config.ts`:
   ```typescript
   server: {
     port: 3002, // измените на другой порт
   }
   ```

### Проблема: "Cannot find module"

**Решение:** Установите зависимости:
```bash
cd admin-panel
npm install
```

### Проблема: Backend недоступен

**Решение:** Убедитесь, что Backend запущен на порту 3000:
```bash
cd backend
npm run dev
```

## 📝 Быстрая команда для запуска всего

Если у вас установлен `concurrently`, можете запустить все сразу:

```bash
# Установите concurrently (один раз)
npm install -g concurrently

# Затем в корне проекта
concurrently "cd backend && npm run dev" "cd admin-panel && npm run dev"
```

## ✅ Готово!

После запуска обоих сервисов:
- ✅ Backend работает на `http://172.20.10.2:3000`
- ✅ Админ-панель работает на `http://172.20.10.2:3001`
- ✅ Можете войти с `admin` / `admin123`

