# DarkCase Admin Panel

Веб-приложение для управления контентом DarkCase Telegram Mini App.

## 🚀 Быстрый старт

### Требования
- Node.js 18+
- npm или yarn
- Запущенный Backend API (см. `../backend/README.md`)

### Установка

1. Установите зависимости:
```bash
npm install
```

2. Создайте файл `.env`:
```bash
cp .env.example .env
```

3. Настройте переменные окружения в `.env`:
```env
VITE_API_URL=http://localhost:3000
```

4. Запустите dev сервер:
```bash
npm run dev
```

Админ-панель автоматически откроется в браузере по адресу: `http://localhost:3001`

**Важно:** Убедитесь, что Backend API запущен на `http://localhost:3000` перед запуском админ-панели!

## 📁 Структура проекта

```
admin-panel/
├── src/
│   ├── api/              # API клиент
│   ├── components/       # React компоненты
│   ├── pages/            # Страницы
│   │   ├── cases/        # Управление кейсами
│   │   └── categories/   # Управление категориями
│   ├── store/            # Zustand store
│   └── App.tsx           # Главный компонент
└── package.json
```

## 🔐 Авторизация

По умолчанию:
- **Username**: `admin`
- **Password**: `admin123`

(Настраивается через переменные окружения бэкенда: `ADMIN_USERNAME`, `ADMIN_PASSWORD`)

## 📝 Функционал

### Dashboard
- Статистика (количество кейсов, пользователей, просмотров)
- Графики просмотров
- Последние добавленные кейсы

### Управление кейсами
- Просмотр списка всех кейсов
- Создание нового кейса
- Редактирование существующего кейса
- Удаление кейса
- Поиск и фильтрация

### Управление категориями
- Просмотр всех категорий
- Создание категорий
- Редактирование категорий
- Удаление категорий

### Загрузка медиа
- Загрузка постеров
- Загрузка backdrop изображений
- Загрузка видео файлов
- Превью загруженных файлов

## 🛠️ Разработка

### Запуск в dev режиме
```bash
npm run dev
```

### Сборка для production
```bash
npm run build
```

### Превью production сборки
```bash
npm run preview
```

## 🚀 Деплой

Админ-панель деплоится отдельно от основного приложения.

### Vercel
```bash
vercel --prod
```

### Настройка переменных окружения
- `VITE_API_URL` - URL бэкенд API

## 🔒 Безопасность

- JWT токены хранятся в localStorage
- Автоматический logout при истечении токена
- Защищенные роуты (требуют авторизации)

## 📦 Технологии

- **React 18** - UI библиотека
- **TypeScript** - Типизация
- **Vite** - Сборщик
- **React Router** - Роутинг
- **React Query** - Управление состоянием API
- **React Hook Form** - Формы
- **Zod** - Валидация
- **Tailwind CSS** - Стилизация
- **Recharts** - Графики
- **Lucide React** - Иконки
- **Zustand** - State management

