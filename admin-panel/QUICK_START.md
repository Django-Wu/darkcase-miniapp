# 🚀 Быстрый старт админ-панели

## Запуск админ-панели

### 1. Убедитесь, что Backend запущен

Сначала запустите Backend API (должен работать на порту 3000):

```bash
cd backend
npm run dev
```

Backend должен быть доступен на `http://localhost:3000`

### 2. Запустите админ-панель

В отдельном терминале:

```bash
cd admin-panel
npm run dev
```

### 3. Откройте в браузере

После запуска админ-панель автоматически откроется в браузере, или откройте вручную:

**URL:** `http://localhost:3001`

### 4. Войдите в систему

**Учетные данные по умолчанию:**
- **Username:** `admin`
- **Password:** `admin123`

(Настраивается через переменные окружения бэкенда: `ADMIN_USERNAME`, `ADMIN_PASSWORD`)

## 🔧 Если админ-панель не открывается

### Вариант 1: Использовать флаг --host

```bash
npm run dev -- --host
```

### Вариант 2: Проверить, что порт свободен

Убедитесь, что порт 3001 не занят другим приложением:

```bash
# Windows
netstat -ano | findstr :3001

# Linux/Mac
lsof -i :3001
```

### Вариант 3: Изменить порт

Если порт 3001 занят, измените его в `vite.config.ts`:

```typescript
server: {
  port: 3002, // или другой свободный порт
  // ...
}
```

## 📝 Проверка работы

1. ✅ Backend запущен на `http://localhost:3000`
2. ✅ Админ-панель запущена на `http://localhost:3001`
3. ✅ Браузер открыт на `http://localhost:3001`
4. ✅ Можете войти с учетными данными `admin` / `admin123`

## 🐛 Решение проблем

### Ошибка "Cannot connect to API"
- Убедитесь, что Backend запущен на порту 3000
- Проверьте, что в `admin-panel/.env` указан правильный `VITE_API_URL`

### Ошибка "Port already in use"
- Закройте другое приложение на порту 3001
- Или измените порт в `vite.config.ts`

### Ошибка авторизации
- Проверьте, что Backend запущен
- Убедитесь, что в Backend настроены переменные `ADMIN_USERNAME` и `ADMIN_PASSWORD`

