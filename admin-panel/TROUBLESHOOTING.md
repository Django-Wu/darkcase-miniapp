# 🔧 Решение проблем админ-панели

## Ошибка "Ошибка авторизации"

### Проблема: Не удается войти в систему

### Решения:

#### 1. Проверьте, что Backend запущен

**Проверка:**
```bash
# Откройте в браузере или используйте curl
curl http://localhost:3000/health
```

Должен вернуться ответ `{"status":"ok"}` или похожий.

**Если Backend не запущен:**
```bash
cd backend
npm run dev
```

Backend должен запуститься на `http://localhost:3000`

#### 2. Проверьте переменные окружения Backend

Убедитесь, что в `backend/.env` есть:
```env
ADMIN_USERNAME=admin
ADMIN_PASSWORD=admin123
JWT_SECRET=your-secret-key-change-in-production
```

Если файла `.env` нет, создайте его:
```bash
cd backend
cp env.example .env
```

#### 3. Проверьте CORS настройки

В `backend/.env` должно быть:
```env
CORS_ORIGIN=http://localhost:5173,http://localhost:3001
```

#### 4. Проверьте консоль браузера

Откройте DevTools (F12) → вкладка Console и Network:
- Проверьте, есть ли ошибки в Console
- Проверьте, отправляется ли запрос на `/api/admin/auth/login` в Network
- Посмотрите статус ответа (должен быть 200, не 401 или 500)

#### 5. Проверьте логи Backend

В терминале, где запущен Backend, должны быть логи:
```
🚀 DarkCase API server running on port 3000
POST /api/admin/auth/login
```

Если видите ошибки - исправьте их.

### Типичные ошибки:

#### "ECONNREFUSED" или "Network Error"
- **Причина:** Backend не запущен или недоступен
- **Решение:** Запустите Backend на порту 3000

#### "401 Unauthorized" или "Invalid credentials"
- **Причина:** Неправильные учетные данные
- **Решение:** Используйте `admin` / `admin123` (или проверьте `ADMIN_USERNAME` и `ADMIN_PASSWORD` в `.env`)

#### "CORS policy" ошибка
- **Причина:** Backend не разрешает запросы с админ-панели
- **Решение:** Добавьте `http://localhost:3001` в `CORS_ORIGIN` в `backend/.env`

#### "500 Internal Server Error"
- **Причина:** Ошибка на сервере (часто проблема с JWT_SECRET)
- **Решение:** Проверьте логи Backend, убедитесь что `JWT_SECRET` установлен в `.env`

## Проверка работоспособности

### Шаг 1: Проверьте Backend
```bash
curl http://localhost:3000/health
```

### Шаг 2: Проверьте endpoint авторизации
```bash
curl -X POST http://localhost:3000/api/admin/auth/login \
  -H "Content-Type: application/json" \
  -d '{"username":"admin","password":"admin123"}'
```

Должен вернуться JSON с `token` и `admin`.

### Шаг 3: Проверьте админ-панель
1. Откройте `http://localhost:3001`
2. Откройте DevTools (F12)
3. Вкладка Network → попробуйте войти
4. Проверьте запрос к `/api/admin/auth/login`

## Быстрая диагностика

Запустите все три сервиса в отдельных терминалах:

**Терминал 1 - Backend:**
```bash
cd backend
npm run dev
```

**Терминал 2 - Админ-панель:**
```bash
cd admin-panel
npm run dev
```

**Терминал 3 - Проверка (опционально):**
```bash
# Проверка Backend
curl http://localhost:3000/health

# Проверка авторизации
curl -X POST http://localhost:3000/api/admin/auth/login \
  -H "Content-Type: application/json" \
  -d '{"username":"admin","password":"admin123"}'
```

Если все работает, вы должны увидеть:
- ✅ Backend запущен на порту 3000
- ✅ Админ-панель запущена на порту 3001
- ✅ Запрос авторизации возвращает токен

