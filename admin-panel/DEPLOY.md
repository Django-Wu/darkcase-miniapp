# 🚀 Деплой админ-панели на admin.darkcase.app

## 📋 Подготовка

### 1. Настройте переменные окружения

Создайте файл `.env.production` в папке `admin-panel/`:

```env
VITE_API_URL=https://api.darkcase.app
```

**Важно:** Замените `https://api.darkcase.app` на URL вашего Backend API.

### 2. Обновите CORS на Backend

В файле `backend/.env` добавьте:

```env
CORS_ORIGIN=https://admin.darkcase.app,https://darkcase.app
```

Или для production:

```env
CORS_ORIGIN=https://admin.darkcase.app,https://darkcase.app,https://your-telegram-miniapp.com
```

## 🌐 Деплой на Vercel (рекомендуется)

### Вариант 1: Через Vercel Dashboard

1. **Подключите репозиторий:**
   - Зайдите на [vercel.com](https://vercel.com)
   - Нажмите "New Project"
   - Подключите ваш GitHub/GitLab/Bitbucket репозиторий

2. **Настройте проект:**
   - **Root Directory:** `admin-panel`
   - **Framework Preset:** Vite
   - **Build Command:** `npm run build`
   - **Output Directory:** `dist`
   - **Install Command:** `npm install`

3. **Настройте домен:**
   - В настройках проекта → Domains
   - Добавьте домен: `admin.darkcase.app`
   - Следуйте инструкциям для настройки DNS

4. **Добавьте переменные окружения:**
   - Settings → Environment Variables
   - Добавьте: `VITE_API_URL` = `https://api.darkcase.app`

5. **Деплой:**
   - Нажмите "Deploy"
   - Дождитесь завершения сборки

### Вариант 2: Через Vercel CLI

1. **Установите Vercel CLI:**
   ```bash
   npm i -g vercel
   ```

2. **Войдите в Vercel:**
   ```bash
   vercel login
   ```

3. **Перейдите в папку админ-панели:**
   ```bash
   cd admin-panel
   ```

4. **Деплой:**
   ```bash
   vercel --prod
   ```

5. **Настройте домен:**
   ```bash
   vercel domains add admin.darkcase.app
   ```

## 🔧 Настройка DNS

Для домена `admin.darkcase.app`:

1. **Добавьте CNAME запись:**
   ```
   Type: CNAME
   Name: admin
   Value: cname.vercel-dns.com (или значение, которое даст Vercel)
   TTL: 3600
   ```

2. **Или добавьте A запись** (если Vercel предоставит IP):
   ```
   Type: A
   Name: admin
   Value: [IP адрес от Vercel]
   TTL: 3600
   ```

## ✅ Проверка после деплоя

1. **Откройте админ-панель:**
   ```
   https://admin.darkcase.app
   ```

2. **Проверьте подключение к API:**
   - Откройте консоль браузера (F12)
   - Должно быть: `🔧 API URL: https://api.darkcase.app`

3. **Попробуйте войти:**
   - Username: `admin`
   - Password: ваш пароль из `backend/.env`

## 🐛 Решение проблем

### Проблема: CORS ошибка

**Решение:**
1. Проверьте `backend/.env` - должен быть `CORS_ORIGIN` с `https://admin.darkcase.app`
2. Перезапустите Backend
3. Проверьте, что Backend доступен по `https://api.darkcase.app`

### Проблема: "Failed to fetch"

**Решение:**
1. Проверьте переменную `VITE_API_URL` в Vercel
2. Убедитесь, что Backend API доступен
3. Проверьте Network tab в браузере для деталей ошибки

### Проблема: Домен не работает

**Решение:**
1. Проверьте DNS настройки (может занять до 48 часов)
2. Проверьте SSL сертификат в Vercel
3. Убедитесь, что домен правильно настроен в Vercel

## 🔐 Безопасность

После деплоя:

1. ✅ Используйте HTTPS (Vercel автоматически)
2. ✅ Обновите пароль админа на более сложный
3. ✅ Настройте rate limiting на Backend
4. ✅ Включите мониторинг и логирование

## 📝 Структура после деплоя

```
admin.darkcase.app (Vercel)
    ↓
    API запросы
    ↓
api.darkcase.app (Backend)
    ↓
    Database (MySQL)
```

## 🎉 Готово!

После успешного деплоя админ-панель будет доступна по адресу:
**https://admin.darkcase.app**

