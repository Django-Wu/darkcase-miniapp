# DarkCase Telegram Bot

Telegram бот для запуска DarkCase Mini App в полноэкранном режиме.

## 🚀 Быстрый старт

### 1. Создание бота

1. Откройте [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте команду `/newbot`
3. Следуйте инструкциям для создания бота
4. Скопируйте полученный токен бота

### 2. Настройка WebApp в BotFather

1. Отправьте команду `/newapp` в BotFather
2. Выберите вашего бота
3. Укажите:
   - **Title**: DarkCase
   - **Description**: Streaming platform
   - **Photo**: загрузите иконку (512x512px)
   - **Web App URL**: `https://darkcase.vercel.app`
   - **Short name**: `darkcase`

4. **Важно**: Включите **Fullscreen mode** в настройках WebApp

### 3. Установка зависимостей

```bash
npm install
```

### 4. Настройка переменных окружения

Создайте файл `.env` в корне проекта:

```bash
BOT_TOKEN=your_bot_token_here
```

Замените `your_bot_token_here` на токен, полученный от BotFather.

### 5. Запуск бота

```bash
npm run bot
```

Для разработки с автоперезагрузкой:

```bash
npm run bot:dev
```

## 📱 Использование

После запуска бота:

1. Найдите вашего бота в Telegram
2. Отправьте команду `/start` или `/app`
3. Нажмите на кнопку **"Open DarkCase"**
4. Приложение откроется в полноэкранном режиме

## 🔧 Команды бота

- `/start` - приветствие и кнопка для открытия WebApp
- `/app` - кнопка для открытия WebApp

## ⚙️ Настройка URL

Если вы используете другой URL для WebApp, измените его в файле `bot.js`:

```javascript
web_app: { url: 'https://your-app-url.com' }
```

## 🐛 Решение проблем

### Бот не отвечает

- Убедитесь, что токен бота правильный
- Проверьте, что бот запущен (`npm run bot`)
- Проверьте логи на наличие ошибок

### WebApp не открывается в полноэкранном режиме

1. Убедитесь, что в BotFather включен **Fullscreen mode**
2. Проверьте, что в `bot.js` используется формат `web_app: { url: ... }`
3. Убедитесь, что в приложении вызываются `WebApp.ready()` и `WebApp.expand()`

### WebApp открывается как панель

- Убедитесь, что в BotFather для WebApp установлен режим **Fullscreen**
- Проверьте, что используется именно WebApp кнопка, а не обычная URL кнопка
- Убедитесь, что URL начинается с `https://`

## 📝 Структура

```
DarkCase/
├── bot.js              # Код Telegram бота
├── .env                # Переменные окружения (не в git)
├── .env.example        # Пример файла окружения
└── package.json        # Зависимости и скрипты
```

## 🔒 Безопасность

- **Никогда не коммитьте** файл `.env` в git
- Храните токен бота в секрете
- Используйте переменные окружения для конфиденциальных данных

## 📚 Дополнительные ресурсы

- [Telegram Bot API](https://core.telegram.org/bots/api)
- [Telegraf Documentation](https://telegraf.js.org/)
- [Telegram WebApp API](https://core.telegram.org/bots/webapps)

